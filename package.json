{
  "name": "blog-koa-mongo",
  "version": "1.0.0",
  "description": "My spectacular Nuxt.js project",
  "author": "LibraMu",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=dev nodemon server/index.js --watch server",
    "build": "nuxt build",
    "build-prod-up": "cross-env NODE_ENV=prod nuxt build && node uploadDeploy.js",
    "prod-up": "node uploadDeploy.js",
    "start": "cross-env NODE_ENV=prod && nuxt start",
    "build:html-test": "cross-env NODE_ENV=test && nuxt generate",
    "build:html-prod": "cross-env NODE_ENV=prod && nuxt generate"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "cross-env": "^5.2.0",
    "koa": "^2.6.2",
    "koa2-cors": "^2.0.6",
    "nuxt": "^2.15.8",
    "ufo": "^0.6.10",
    "view-design": "^4.5.0"
  },
  "devDependencies": {
    "compression-webpack-plugin": "^5.0.2",
    "nodemon": "^1.18.9",
    "request": "^2.88.2"
  }
}
